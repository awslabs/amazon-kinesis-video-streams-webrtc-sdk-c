# Target Firmware

Place the following ESP firmware binaries in this directory:

- `bootloader.bin` - ESP bootloader binary
- `partition-table.bin` - Partition table configuration
- `app.bin` - Main application binary

These binaries can be compiled from the test/target-example-src folder, or can be manually copied from your ESP-IDF build output.
